# Core runtime
streamlit>=1.20.0
python-dotenv>=1.0.0

# LangChain + HF integration used in app.py
langchain>=0.0.260
langchain-huggingface>=0.0.18

# Hugging Face & ML backends (some are optional if you only use HF Inference API)
huggingface-hub>=0.14.1
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.20.0
